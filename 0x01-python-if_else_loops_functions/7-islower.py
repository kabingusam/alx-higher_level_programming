def islower(c):
    # ord() function returns the number representing the unicode code of a specified character.
    # lowercase c range :from 97 to 122
    if ord(c) => 97  and ord(c) <= 122:
        return true
    else:
        return false